{% extends 'base.html.twig' %}
{% block title %}Ac{% endblock %}
{% block body %}
    <div class="container mt-5 ml-5 col-6 bg-primary d-flex  p-5">
   {{form_widget(form)}}
   <button type="submit" class="btn btn-success">
    {% if editAnneeScolaire %}
        enregistrer
        {% else %}
        Ajouter
        {% endif %}
  </button>
         {{form_end(form) }}
    </div>
{% endblock %}
